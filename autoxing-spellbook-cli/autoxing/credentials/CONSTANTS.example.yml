# Copy to CONSTANTS.yml (gitignored) and edit robots / active_index.
active_index: 0
prefix: http://
timeout_ms: 10000
robots:
  - name: ARTC_L300
    robot_ip: "192.168.68.64:8090"
    # timeout_ms: 15000  # optional per-robot override (milliseconds)
